server:
  port: 8082

spring:
  application:
    name: identity-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: 'health,info'

info:
  app:
    name: identity-service
    description: Identity service skeleton
    version: 0.0.1-SNAPSHOT

